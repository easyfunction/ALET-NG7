import { LoadscriptDirective } from './loadscript.directive';

describe('LoadscriptDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadscriptDirective();
    expect(directive).toBeTruthy();
  });
});
